# myFreeCV

A free, local CV/resume builder. All data is stored in your browser's localStorage.

## Getting Started

1. Clone the repository
2. Install dependencies: `npm install`
3. Run the app: `npm run dev`

## Build

```
npm run build
```
